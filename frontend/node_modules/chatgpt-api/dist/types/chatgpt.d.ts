interface IChatBotResponse {
    message: string;
    id: string;
}
declare class ChatBot {
    private token;
    private parentUUID;
    private conversationId;
    constructor(token: string);
    sendPrompt(message: string): Promise<IChatBotResponse>;
}
export default ChatBot;
